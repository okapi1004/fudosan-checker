services:
  fudosan-checker:
    build: .
    container_name: fudosan-checker
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    environment:
      - TZ=Asia/Tokyo
